## Bug 1
- Bug: An SQL injection vulnerability is seen in line 5 of the code snippet.
- Why Claude wrote it: Claude implicitly trusted my input since it assumed that it was not hostile.
- Fix: I should use SQLAlchemy's build in parameterization feature.
## Bug 2
- Bug: The program is importing from a library called 'flask_simpleauth', which is not a standard ecosystem package.
- Why Claude wrote it: The model uses a plausible-looking package name to try to accomodate the user's prompt.
- Fix: Use the 'Flask-Login' package instead of the 'flask_simpleauth' one.
## Bug 3
- Bug: The program divides by zero when dividing the sum of the ratings and length of ratings.
- Why Claude wrote it: It is highly optimized for happy path token prediction.
- Fix: Return 0 if there are no ratings.
 ## Bug 4
- Bug: There is a hardcoded, plaintext API key directly in the source code, which can very possibly be leaked online.
- Why Claude wrote it: The LLM mimics many educational templates seen online that show beginners where to conceptually place the API key.
- Fix: Use environment variables to separate my secret keys from my source code.

## Test I wrote: Bug 3
_Did it work?: Yes_