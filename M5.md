1. Update the note creation data structure to initialize an empty `tags` list by default. Verify: existing tests pass; a newly created note contains `note["tags"] == []`.
2. Add a helper function to parse comma-separated strings into a cleaned list of lowercased, stripped tags. Verify: input `"work, urgent,   work "` yields `["work", "urgent"]` without duplicates.
3. Update the new-note creation route and form to accept a `tags` string and save the parsed list. Verify: creating a note with tags saves them correctly into the in-memory list.

The plan came back wrong on the first try because it failed to account for the UI dependency of the request. Task 2 did not completely borrow lessons from Task 1, as it did not write a pure utility function and a unit test before entangling it with Flask's request context. If I re-did this, I would detatch the core logic tasks from the web framework tasks.